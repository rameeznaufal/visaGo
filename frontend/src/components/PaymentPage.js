import { useCommonStore } from "../utils/store";

const PaymentPage = () => {
 
    
  //const [location,setLocation]=useState();

  //console.log(offer)
  const {selectedOffer,setSelectedOffer}=useCommonStore();
  return (
    null
  );
};

export default PaymentPage;